# AI Flashcard — Frontend

Run locally:

1. npm install  
2. npm run dev  
3. Open: http://localhost:5173  

Uses:
- React + TypeScript
- TailwindCSS
- Vite

API expected (backend):
- POST /api/upload
- GET /api/flashcards
- GET /api/quiz
